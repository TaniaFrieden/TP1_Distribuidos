FROM python:3-alpine

RUN pip3 install pika docker

WORKDIR /app
ENV PYTHONPATH=/app:/app/base

COPY common ./common
COPY workers/base ./base
COPY watchdog ./watchdog

ENTRYPOINT ["python3", "watchdog/actuador_main.py"]
